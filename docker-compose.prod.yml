services:
  api-gateway:
    ports:
      - "8080:8000"
    environment:
      LOG_LEVEL: WARNING
    restart: always

  writer-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  inventory-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  notification-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  postgres:
    restart: always

  postgres-notification:
    restart: always

  redis:
    restart: always

  rabbitmq:
    image: rabbitmq:3-alpine
    restart: always
    